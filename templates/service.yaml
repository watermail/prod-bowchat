apiVersion: v1
kind: Service
metadata:
  name: {{ include "prod-bowchat.fullname" . }}
  labels:
    {{- include "prod-bowchat.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "prod-bowchat.selectorLabels" . | nindent 4 }}
